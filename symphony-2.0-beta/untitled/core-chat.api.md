## API Report File for "@mana/core-chat"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ConversationId } from '@mana/core-conversations';
import { IService } from '@mana/core-ui';
import { IView } from '@mana/core-ui';

// @public
export enum DockingMode {
    BOTTOM = "bottom",
    CENTER = "center",
    COVER = "cover",
    FLOAT = "float",
    FOOTER = "footer",
    HEADER = "header",
    LEFT = "left",
    RIGHT = "right",
    TOP = "top"
}

// @public
export interface IChatService extends IService {
    registerOverlayViewFactory(factory: OverlayFactory): void;
}

// @public (undocumented)
export namespace IChatService {
    const // (undocumented)
    TypeTag = "@mana/core-chat/IChatService";
}

// @public
export interface IOverlayView extends IView {
    getDockingMode(): DockingMode | undefined;
    subscribe(fn: () => void): void;
}

// @public
export type OverlayFactory = (conversationId: ConversationId) => Promise<IOverlayView[]>;


// (No @packageDocumentation comment for this package)

```
